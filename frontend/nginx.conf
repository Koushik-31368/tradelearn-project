server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # This line ensures that refreshing on any page works correctly
    try_files $uri $uri/ /index.html;
  }
}